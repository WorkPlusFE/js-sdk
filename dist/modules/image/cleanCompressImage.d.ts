/**
 * 清除压缩后的图片
 * @description 拍照或选择照片后都会生成压缩图片，调用这个方法去清除
 * @param {ExecSyncOptions<[]>} [options]
 * @returns
 */
declare function cleanCompressImage(): void;
export default cleanCompressImage;
