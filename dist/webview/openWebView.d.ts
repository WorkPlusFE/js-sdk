import { OpenWebView } from '../types/webview';
/**
 * 打开一个网页
 * @description 传入地址，打开 WorkPlus 网页
 * @param {OpenWebView} options
 * @module webview
 * @returns 无
 */
declare function openWebView(options: OpenWebView): void;
export default openWebView;
